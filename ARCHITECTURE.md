# Architecture — cost-accounting

> Auto-generated by `/auto setup`. Updated by `/auto sync` after Wave 7 (SPEC-COST-001 complete).
> Last updated: 2026-04-21

## Project

**cost-accounting** — Portfolio prototype demonstrating cost/management accounting:
job-order costing, overhead allocation, transfer pricing, variance analysis, contribution margin.

**Status**: Waves 1–7 complete (all engines + full UI + close workflow + export + performance tests). SPEC-COST-001 closed.

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | Next.js 14 App Router (TypeScript strict) |
| Package manager | pnpm |
| Database ORM | Prisma 5 (PostgreSQL, `@db.Decimal(18,4)` for all monetary fields) |
| Validation | Zod 3.23 (API boundary schemas, all lib/zod/* implemented) |
| Styling | Tailwind CSS v3 + shadcn/ui |
| Charts | Recharts |
| Unit tests | Vitest + @vitest/coverage-v8 |
| E2E tests | Playwright |

## Directory Layout

```
cost-accounting/
├── app/                    # Next.js App Router
│   ├── layout.tsx          # Root layout (PersonaProvider, DemoBanner)
│   ├── page.tsx            # Home page (persona-aware nav hub)
│   ├── globals.css         # Tailwind directives
│   ├── (demo)/
│   │   └── page.tsx        # Demo mode landing page (persona picker)
│   ├── (master)/           # Master data CRUD UI (T10)
│   │   ├── layout.tsx      # Master data layout
│   │   ├── cost-entries/
│   │   │   ├── page.tsx    # CostEntry list + search
│   │   │   └── actions.ts  # Server actions: create/update/delete
│   │   ├── organizations/page.tsx
│   │   ├── personnel/page.tsx
│   │   ├── projects/page.tsx
│   │   ├── standard-rates/page.tsx
│   │   └── transfer-markups/page.tsx
│   ├── close/              # Monthly close workflow (T7)
│   │   ├── page.tsx        # Period selector + run button
│   │   └── actions.ts      # Server action: runClose()
│   └── dashboard/          # Read-only dashboards (T9)
│       ├── enterprise/page.tsx     # Enterprise consolidated P&L
│       ├── hq/[id]/page.tsx        # HQ drilldown
│       ├── project/[id]/page.tsx   # Project budget vs actual
│       ├── personnel/[id]/page.tsx # Personnel utilization
│       └── variance/page.tsx       # Variance factor analysis
├── components/
│   ├── demo/
│   │   ├── DemoBanner.tsx  # Sticky top banner with persona label
│   │   └── PersonaSwitcher.tsx # Dropdown to switch persona
│   ├── charts/
│   │   └── VarianceBarChart.tsx  # Recharts four-way variance bar
│   └── export/
│       └── ExportButton.tsx  # CSV / Excel download button
├── lib/
│   ├── utils.ts            # cn() utility (clsx + tailwind-merge)
│   ├── prisma.ts           # Singleton PrismaClient
│   ├── persona.tsx         # PersonaProvider + usePersona hook (localStorage)
│   ├── audit.ts            # logAudit() — AuditLog writer
│   ├── zod/                # Zod validation schemas (T2)
│   │   ├── costEntry.ts, allocation.ts, transfer.ts
│   │   ├── master.ts, period.ts, _shared.ts
│   ├── aggregation/        # Pure aggregation functions (T3)
│   │   ├── personnel.ts, project.ts, hq.ts, enterprise.ts, types.ts, _helpers.ts
│   ├── allocation/         # Allocation engine (T4, TDD)
│   │   ├── decimal.ts, direct.ts, stepDown.ts
│   │   ├── checksum.ts, runner.ts, runner.queries.ts
│   ├── transfer/           # Transfer pricing engine (T5, TDD)
│   │   ├── rate.ts, engine.ts
│   ├── variance/           # Variance decomposition (T6, TDD)
│   │   ├── decompose.ts, reconcile.ts
│   ├── close/              # Monthly close orchestration (T7)
│   │   └── workflow.ts     # runClose(): allocation + transfer in single txn
│   └── export/             # Export utilities (T11)
│       ├── csv.ts          # CSV serializer (browser-only, papaparse)
│       └── xlsx.ts         # Excel serializer (SheetJS)
├── prisma/
│   ├── schema.prisma       # 14-model schema
│   ├── seed.ts, seed/small.ts, seed/full.ts
│   └── migrations/
├── tests/
│   ├── unit/               # 25 unit test files, 178+ tests, 92% coverage
│   │   ├── utils.test.ts, aggregation/*, allocation/*
│   │   ├── transfer/*, variance/*, zod/*
│   │   ├── audit/audit.test.ts
│   │   ├── close/workflow.test.ts
│   │   └── export/csv.test.ts
│   ├── integration/        # 4 integration test files (DB-dependent, skipped in CI unit run)
│   │   ├── allocation-determinism.test.ts
│   │   ├── allocation-perf.test.ts
│   │   ├── close-workflow.test.ts
│   │   └── reconciliation.test.ts
│   └── e2e/                # Playwright E2E (T12)
│       ├── smoke.spec.ts
│       ├── demo-banner.spec.ts
│       └── drill-enterprise-to-personnel.spec.ts
├── docs/
│   ├── allocation-algorithm.md
│   └── variance-algorithm.md
├── package.json, tsconfig.json, vitest.config.ts, playwright.config.ts
└── .env.example
```

## Domain Model (from SPEC-COST-001 §5)

14 Prisma models in 4 concern groups:

**Master data**: Organization (3-level hierarchy), Project, Personnel, CostCategory, StandardRate, TransferMarkup

**Transactional**: CostEntry (grain: personnel × project × date × period), Period (OPEN/CLOSED state machine)

**Allocation engine**: AllocationRule, AllocationRun (audit), AllocationResult

**Analytics**: TransferEntry, VarianceSnapshot, AuditLog

All monetary fields: `Decimal @db.Decimal(18, 4)` → PostgreSQL `DECIMAL(18,4)`.

## Architecture Layers (Wave 7 final state)

```
UI (app/ + components/):
  app/(dashboard)/     — Enterprise / HQ / Project / Variance / Personnel dashboards  [Wave 5 — ✓]
  app/(master)/        — CRUD pages for all master data entities                      [Wave 5 — ✓]
  app/close/           — Monthly close workflow (server action)                       [Wave 4 — ✓]
  app/(demo)/          — Demo mode landing page + persona picker                      [Wave 4 — ✓]
  components/demo/     — DemoBanner, PersonaSwitcher                                  [Wave 4 — ✓]
  components/charts/   — VarianceBarChart (Recharts)                                  [Wave 5 — ✓]
  components/export/   — ExportButton                                                 [Wave 6 — ✓]

Persona layer:
  lib/persona.tsx      — PersonaProvider + usePersona (localStorage, 4 roles)        [Wave 4 — ✓]

Server actions / API:
  lib/zod/             — Zod schema validation at every input boundary                [Wave 2 — ✓]
  lib/audit.ts         — logAudit() writes AuditLog rows                              [Wave 4 — ✓]

Engines (lib/, pure functions, framework-free):
  lib/close/workflow.ts — runClose(): allocation + transfer in single DB transaction  [Wave 4 — ✓]
  lib/allocation/       — direct.ts, stepDown.ts, runner.ts, decimal.ts, checksum.ts [Wave 3 — ✓]
  lib/transfer/         — rate.ts, engine.ts                                          [Wave 3 — ✓]
  lib/variance/         — decompose.ts, reconcile.ts                                  [Wave 3 — ✓]
  lib/aggregation/      — personnel.ts, project.ts, hq.ts, enterprise.ts             [Wave 2 — ✓]
  lib/export/           — csv.ts (papaparse), xlsx.ts (SheetJS)                      [Wave 6 — ✓]
```

## Key Design Decisions

### Decimal Arithmetic
All monetary computations use Prisma `Decimal` end-to-end. `lib/allocation/decimal.ts` configures `Decimal.precision=28` and patches `Decimal.prototype.plus` to propagate `Money` subclass. A double-patch guard prevents re-entry on multiple imports.

### Checksum Strategy
`lib/allocation/checksum.ts` produces deterministic SHA-256 input/output checksums stored in `AllocationRun`. Canonical sort key: `(fromPoolOrgId, toProjectId)`. Stored digests break if sort order or serialization changes — marked `@AX:ANCHOR`.

### ConservativeAdd
`conservativeAdd` in `decimal.ts` guarantees `sum(allocated) === pool` exactly via last-step rounding adjustment. All allocation callers depend on this post-condition for REQ-ALLOC-01 conservation.

## Key Constraints

- **File size**: 300-line hard limit; 200-line target. Excluded: *.md, *.json, *.yaml.
- **TDD enforced**: T4/T5/T6 engines follow RED → GREEN → REFACTOR.
- **Coverage target**: 80%+ on lib/allocation/ and lib/transfer/ (REQ-NFR-MAINT-02). DB-dependent modules (runner.ts, runner.queries.ts, rate.ts) excluded from unit coverage.
- **Decimal arithmetic**: All monetary computations use Prisma Decimal end-to-end (TC-6).
- **Language policy**: Code comments, commits, and AI outputs in English.

## Implementation Waves

| Wave | Tasks | Status |
|------|-------|--------|
| 1 | T0 (bootstrap), T1 (Prisma schema + seed) | ✓ Complete |
| 2 | T2 (Zod schemas), T3 (aggregation engine) | ✓ Complete |
| 3 | T4 (allocation TDD), T5 (transfer TDD), T6 (variance TDD) | ✓ Complete |
| 4 | T7 (close workflow), T8 (layout + demo mode + persona) | ✓ Complete |
| 5 | T9 (dashboards), T10 (master data UI) | ✓ Complete |
| 6 | T11 (export + polish) | ✓ Complete |
| 7 | T12 (perf + smoke tests, 92% unit coverage) | ✓ Complete |

## Rule Violations

None detected. Notable file sizes (all under 300-line hard limit):
- `prisma/schema.prisma` — ~244 lines
- `prisma/seed/full.ts` — ~263 lines
- `lib/allocation/runner.queries.ts` — within limit (N+1 query pattern tagged @AX:WARN)
